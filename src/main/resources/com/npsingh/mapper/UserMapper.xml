<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.npsingh.mapper.UserMapper">

	<select id="getUsers" resultType="com.npsingh.model.User">
		select * from user
	</select>

	<select id="getUser" resultType="com.npsingh.model.User">
		select * from user where id=
		#{id}
	</select>

	<insert id="insertUser" parameterType="com.npsingh.model.User"
		keyProperty="id" keyColumn="id" useGeneratedKeys="true">
		insert into
		user(id,firstname,lastname,email,dob,mobile)
		values(#{id},#{firstname},#{lastname},#{email},#{dob},#{mobile})
	</insert>

	<update id="updateUser" parameterType="com.npsingh.model.User">
		update user
		set firstname = #{firstname}, lastname = #{lastname}, email = #{email},
		dob = #{dob},mobile = #{mobile} where id= #{id}
	</update>
	
	<delete id="deleteUser">
		delete from user where id = #{id}
	</delete>

</mapper>